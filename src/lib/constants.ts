export const PDF_MIME_TYPE = "application/pdf";
export const PDF_FILE_EXTENSION = ".pdf";

/** Value for the file-input `accept` attribute when picking PDFs. */
export const PDF_UPLOAD_ACCEPT = `${PDF_MIME_TYPE},${PDF_FILE_EXTENSION}`;

/** Shared toast description for retryable failures. */
export const TRY_AGAIN_DESCRIPTION = "Please try again.";
